"use strict";

const { statpagesRouter, getServiceName, buildUrl, checkExistUrl } = require('./router')

module.exports = {
    statpagesRouter,
    getServiceName,
    buildUrl,
    checkExistUrl,
}
