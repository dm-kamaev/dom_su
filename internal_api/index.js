"use strict";

const { internalServiceAPI } = require('./serviceRouter')
const { internalClientAPI } = require('./clientRouter')

module.exports = {
    internalServiceAPI,
    internalClientAPI
}

